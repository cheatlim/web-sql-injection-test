package com.security.sqli.core.model;

/**
 * HTTP methods supported for SQL injection testing.
 */
public enum HttpMethod {
    GET,
    POST,
    PUT,
    DELETE,
    PATCH,
    HEAD,
    OPTIONS
}
